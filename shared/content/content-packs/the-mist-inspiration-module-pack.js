/**
 * Retired Phase 8 public-registry bridge.
 * Production Archive assembly now uses a catalog isolated from semantic
 * migration selection, so The Mist remains public without a dedicated bridge.
 */
export const THE_MIST_INSPIRATION_MODULE_CONTENT_PACK_STATUS = "retired";
