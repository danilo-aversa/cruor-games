/**
 * Retired Phase 8 public-registry bridge.
 * Production Archive assembly now uses a catalog isolated from semantic
 * migration selection, so Wolf Spiders remains public without this bridge.
 */
export const WOLF_SPIDERS_INSPIRATION_MODULE_CONTENT_PACK_STATUS = "retired";
